# Market Aggregation Service Configuration Example
# Copy this to .env and fill in your values

# Kalshi API Configuration (optional but recommended)
# Get your API key from https://kalshi.com
KALSHI_API_KEY=your_kalshi_api_key_here
KALSHI_PRIVATE_KEY_PATH=./rsa_key

# API Server Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Price Tracker Configuration
UPDATE_INTERVAL_SECONDS=5

# Notes:
# - Polymarket and Limitless don't require authentication for read-only access
# - Kalshi authentication is optional but provides better rate limits
# - Generate RSA key pair for Kalshi using: ssh-keygen -t rsa -b 2048 -m PEM -f rsa_key

